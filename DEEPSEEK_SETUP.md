# 🚀 DeepSeek - Настройка и использование

## 🎯 Почему DeepSeek?

| Параметр | DeepSeek | Claude 3.5 |
|----------|----------|------------|
| **Цена входных токенов** | $0.14 / 1M | $3.00 / 1M |
| **Цена выходных токенов** | $0.28 / 1M | $15.00 / 1M |
| **Экономия** | **В 20 раз дешевле!** | - |
| **Качество** | Отличное | Отличное |
| **Скорость** | Быстро | Быстро |

**100 RAG запросов:**
- DeepSeek: ~$0.05 (5 центов!)
- Claude: ~$1.00

---

## ⚡ Быстрая настройка (2 минуты)

### Шаг 1: Получите API ключ

1. Зайдите на https://platform.deepseek.com/
2. Создайте аккаунт
3. API Keys → Create API Key
4. Скопируйте ключ

### Шаг 2: Настройте проект

```bash
# Откройте/создайте файл .env.local
nano .env.local

# Добавьте:
LLM_PROVIDER=deepseek
DEEPSEEK_API_KEY=sk-ваш-ключ-здесь

# Сохраните (Ctrl+O, Enter, Ctrl+X)
```

### Шаг 3: Перезапустите

```bash
npm run dev
```

### Шаг 4: Проверьте

Откройте http://localhost:3000 и попробуйте RAG!

В логах должно быть:
```
[Config] Используется модель по умолчанию: deepseek-chat
[LLM] Отправка запроса к DeepSeek API...
```

**Готово!** ✅

---

## 📋 Доступные модели DeepSeek

### 1. deepseek-chat (рекомендуется)
- Основная модель
- Отличное качество
- Быстрая
- **Цена:** $0.14 вх + $0.28 вых / 1M токенов

```bash
LLM_PROVIDER=deepseek
DEEPSEEK_API_KEY=ваш-ключ
LLM_MODEL=deepseek-chat
```

### 2. deepseek-reasoner
- Модель с глубоким рассуждением (как GPT-o1)
- Для сложных задач
- Медленнее, но очень умная
- **Цена:** $0.55 вх + $2.19 вых / 1M токенов

```bash
LLM_PROVIDER=deepseek
DEEPSEEK_API_KEY=ваш-ключ
LLM_MODEL=deepseek-reasoner
```

---

## 🔄 Переключение между провайдерами

### Использовать DeepSeek:
```bash
# .env.local
LLM_PROVIDER=deepseek
DEEPSEEK_API_KEY=ваш-ключ
```

### Использовать Claude:
```bash
# .env.local
LLM_PROVIDER=anthropic
ANTHROPIC_API_KEY=sk-ant-ваш-ключ
```

**Перезапустите:** `npm run dev`

---

## 💰 Сравнение стоимости

### Пример: 100 RAG запросов (с документами)

**DeepSeek:**
- Входные токены: 100 × 2000 = 200K
- Выходные токены: 100 × 500 = 50K
- Стоимость: (200K × $0.14 + 50K × $0.28) / 1M = **$0.04**

**Claude:**
- Входные токены: 100 × 2000 = 200K
- Выходные токены: 100 × 500 = 50K
- Стоимость: (200K × $3 + 50K × $15) / 1M = **$1.35**

**Экономия: 97%!** 🎉

---

## 🎨 Пример .env.local

### Минимальная конфигурация:
```bash
LLM_PROVIDER=deepseek
DEEPSEEK_API_KEY=sk-ваш-ключ-здесь
```

### Полная конфигурация:
```bash
# Провайдер
LLM_PROVIDER=deepseek

# API ключ
DEEPSEEK_API_KEY=sk-ваш-deepseek-ключ

# Модель (опционально, по умолчанию deepseek-chat)
LLM_MODEL=deepseek-chat

# Можно держать оба ключа и переключаться
ANTHROPIC_API_KEY=sk-ant-ваш-claude-ключ
```

---

## 🔍 Проверка работы

### Проверьте статус:
```bash
curl http://localhost:3000/api/rag/status
```

Должно показать:
```json
{
  "components": {
    "llm_provider": {
      "configured": true,
      "provider": "deepseek",
      "model": "deepseek-chat"
    }
  }
}
```

### Тестовый запрос:
```bash
curl -X POST http://localhost:3000/api/rag \
  -H "Content-Type: application/json" \
  -d '{"query": "Привет!", "useRAG": false}'
```

---

## ⚙️ Переключение моделей

### Попробовать reasoner для сложной задачи:
```bash
# В .env.local измените:
LLM_MODEL=deepseek-reasoner

# Перезапустите
npm run dev
```

### Вернуться на chat:
```bash
# В .env.local:
LLM_MODEL=deepseek-chat

# Перезапустите
npm run dev
```

---

## 📊 Когда использовать что?

### deepseek-chat (по умолчанию):
- ✅ RAG вопросы по документам
- ✅ Обычные вопросы
- ✅ Генерация текста
- ✅ 95% задач

### deepseek-reasoner:
- ✅ Сложная логика и рассуждения
- ✅ Математика
- ✅ Программирование
- ✅ Анализ
- ⚠️ Дороже и медленнее

### Claude:
- ✅ Если нужна именно Claude
- ✅ Специфичные задачи
- ⚠️ Дороже

---

## 🐛 Troubleshooting

### Ошибка: "DEEPSEEK_API_KEY не установлен"
```bash
# Проверьте файл
cat .env.local

# Должна быть строка:
# DEEPSEEK_API_KEY=sk-...

# Перезапустите
npm run dev
```

### Ошибка: 401 Unauthorized
- Проверьте ключ на https://platform.deepseek.com/
- Убедитесь что ключ активен
- Нет лишних пробелов в .env.local

### Ошибка: 429 Rate Limit
- Слишком много запросов
- Подождите минуту
- Проверьте лимиты в аккаунте

### Медленные ответы
- Это нормально для первого запроса (cold start)
- Дальше будет быстрее
- Попробуйте deepseek-chat вместо reasoner

---

## 🎉 Готово!

Теперь у вас:
- ✅ Работающий RAG с DeepSeek
- ✅ В 20 раз дешевле чем Claude
- ✅ Отличное качество ответов
- ✅ Возможность переключаться между провайдерами

**Экономьте и наслаждайтесь!** 🚀

---

## 📚 Дополнительная информация

- DeepSeek Docs: https://platform.deepseek.com/docs
- Pricing: https://platform.deepseek.com/docs/pricing
- API Keys: https://platform.deepseek.com/api-keys

**Вопросы?** Смотрите:
- `RAG_DOCUMENTATION.md` - полная документация
- `TROUBLESHOOTING.md` - решение проблем
