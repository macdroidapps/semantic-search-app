# 🤖 Настройка DeepSeek AI

Это руководство поможет настроить интеграцию с DeepSeek AI для ответов на вопросы по вашим документам.

## 📋 Что такое DeepSeek интеграция?

DeepSeek - это мощная языковая модель, которая теперь может отвечать на вопросы на основе ваших индексированных документов:

1. Вы задаёте вопрос
2. Система находит релевантные фрагменты в документах
3. DeepSeek читает эти фрагменты и даёт осмысленный ответ
4. Вы получаете точный ответ со ссылками на источники

## 🔑 Получение API ключа

### Шаг 1: Регистрация
1. Перейдите на [https://platform.deepseek.com/](https://platform.deepseek.com/)
2. Зарегистрируйтесь или войдите в аккаунт

### Шаг 2: Создание API ключа
1. Перейдите в раздел "API Keys"
2. Нажмите "Create API Key"
3. Дайте ключу название (например, "Semantic Search App")
4. Скопируйте созданный ключ

### Шаг 3: Пополнение баланса (опционально)
DeepSeek предоставляет бесплатные кредиты при регистрации. Если они закончатся, пополните баланс на странице биллинга.

## ⚙️ Настройка проекта

### 1. Создайте файл .env

В корне проекта создайте файл `.env`:

```bash
cp .env.example .env
```

### 2. Добавьте API ключ

Откройте файл `.env` и добавьте ваш ключ:

```env
DEEPSEEK_API_KEY=sk-ваш-ключ-здесь
```

### 3. Перезапустите сервер

```bash
# Остановите текущий сервер (Ctrl+C)
# Запустите заново
npm run dev
```

## 🚀 Использование

### Через Web UI

1. Откройте приложение: http://localhost:3000
2. Переключитесь на режим **"🤖 DeepSeek AI"**
3. Убедитесь, что индекс создан (кнопка "Переиндексировать")
4. Задайте вопрос в поле ввода
5. Нажмите **"🤖 Спросить DeepSeek"**

Примеры вопросов:
- "Что такое семантический поиск и как он работает?"
- "Объясни разницу между supervised и unsupervised learning"
- "Как использовать Server Components в Next.js?"

### Через API

```bash
curl -X POST http://localhost:3000/api/ask \
  -H "Content-Type: application/json" \
  -d '{
    "question": "Что такое семантический поиск?",
    "top_k": 5,
    "min_score": 0.2
  }'
```

#### Параметры запроса

| Параметр | Тип | Обязательный | По умолчанию | Описание |
|----------|-----|--------------|--------------|----------|
| `question` | string | Да | - | Вопрос пользователя |
| `top_k` | number | Нет | 5 | Количество фрагментов для контекста (1-20) |
| `min_score` | number | Нет | 0.2 | Минимальный порог релевантности (0-1) |

#### Пример ответа

```json
{
  "success": true,
  "question": "Что такое семантический поиск?",
  "answer": "Семантический поиск - это метод поиска информации, который учитывает смысл и контекст запроса, а не только точное совпадение ключевых слов...",
  "context": [
    {
      "index": 1,
      "text": "Семантический поиск использует эмбеддинги...",
      "source": "semantic-search.md",
      "score": 0.8523
    }
  ],
  "stats": {
    "context_chunks": 5,
    "duration_seconds": 2.345
  }
}
```

## 💡 Примеры использования

### JavaScript/TypeScript

```typescript
async function askQuestion(question: string) {
  const response = await fetch('http://localhost:3000/api/ask', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ question }),
  });

  const data = await response.json();

  console.log('Вопрос:', data.question);
  console.log('Ответ:', data.answer);
  console.log('Использовано фрагментов:', data.stats.context_chunks);
}

// Использование
await askQuestion('Как работает машинное обучение?');
```

### Python

```python
import requests

def ask_question(question: str):
    response = requests.post(
        'http://localhost:3000/api/ask',
        json={'question': question}
    )
    data = response.json()

    print(f"Вопрос: {data['question']}")
    print(f"Ответ: {data['answer']}")
    print(f"Использовано фрагментов: {data['stats']['context_chunks']}")

# Использование
ask_question('Как работает машинное обучение?')
```

## 📊 Стоимость использования

DeepSeek имеет конкурентные цены:

- **deepseek-chat**: ~$0.14 за 1M входных токенов, ~$0.28 за 1M выходных токенов
- Средний запрос: 500-1000 входных токенов, 200-500 выходных токенов
- Примерная стоимость одного запроса: $0.0001-0.0003 (0.01-0.03 цента)

Актуальные цены: [https://platform.deepseek.com/pricing](https://platform.deepseek.com/pricing)

## 🔧 Настройка параметров

### Изменение модели

По умолчанию используется `deepseek-chat`. Чтобы изменить модель, отредактируйте `lib/deepseek.ts`:

```typescript
const DEFAULT_MODEL = 'deepseek-chat'; // Измените на другую модель
```

### Настройка промпта

Системный промпт настраивается в функции `createContextualPrompt` в файле `lib/deepseek.ts`. Вы можете изменить инструкции для модели там.

### Настройка temperature

Temperature контролирует креативность ответов:
- `0.0` - более детерминированные ответы
- `0.7` - сбалансированные ответы (по умолчанию)
- `1.0` - более креативные ответы

Изменить можно в вызове `askDeepSeek`:

```typescript
const answer = await askDeepSeek(messages, {
  temperature: 0.5, // Ваше значение
});
```

## 🐛 Решение проблем

### Ошибка: "DEEPSEEK_API_KEY не найден"

**Решение:**
1. Проверьте, что файл `.env` создан в корне проекта
2. Убедитесь, что в `.env` есть строка `DEEPSEEK_API_KEY=ваш-ключ`
3. Перезапустите сервер после создания `.env`

### Ошибка: "DeepSeek API error: 401"

**Решение:**
1. Проверьте правильность API ключа
2. Убедитесь, что ключ активен на платформе DeepSeek
3. Проверьте баланс аккаунта

### Ошибка: "Индекс не найден"

**Решение:**
1. Убедитесь, что документы добавлены в `data/documents/`
2. Запустите индексацию через UI или API: `POST /api/index`

### Медленные ответы

**Возможные причины:**
1. Большое количество `top_k` - уменьшите до 3-5
2. Длинные документы - DeepSeek обрабатывает много токенов
3. Сетевая задержка - проверьте подключение

**Оптимизация:**
```json
{
  "question": "ваш вопрос",
  "top_k": 3,
  "min_score": 0.4
}
```

### Нерелевантные ответы

**Решение:**
1. Убедитесь, что документы содержат нужную информацию
2. Увеличьте `min_score` для более релевантных фрагментов
3. Переформулируйте вопрос более конкретно
4. Проверьте, что индекс актуальный (переиндексируйте при необходимости)

## 🔒 Безопасность

1. **Никогда не коммитьте .env файл** в git
2. `.env` уже добавлен в `.gitignore`
3. **Не делитесь API ключом** публично
4. Для продакшена используйте переменные окружения хостинга

## 📈 Мониторинг использования

Отслеживайте использование API на [платформе DeepSeek](https://platform.deepseek.com/usage):
- Количество запросов
- Использованные токены
- Стоимость
- История запросов

## 🎯 Лучшие практики

1. **Хорошие вопросы:**
   - Конкретные: "Как работает косинусное сходство в семантическом поиске?"
   - Контекстные: "Объясни преимущества Server Components в Next.js"
   - Сравнительные: "В чём разница между supervised и unsupervised learning?"

2. **Плохие вопросы:**
   - Слишком общие: "Расскажи про AI"
   - Не связанные с документами: "Какая погода в Москве?"
   - Многосоставные: "Расскажи про ML, DL, NLP и CV одновременно"

3. **Оптимизация:**
   - Используйте `top_k: 3-5` для большинства вопросов
   - Увеличивайте `min_score` для более точных результатов
   - Обновляйте индекс при добавлении новых документов

## 📚 Дополнительная информация

- [DeepSeek Documentation](https://platform.deepseek.com/docs)
- [DeepSeek API Reference](https://platform.deepseek.com/api-docs)
- [Pricing](https://platform.deepseek.com/pricing)

## 🆘 Поддержка

Если возникли проблемы:
1. Проверьте этот документ
2. Посмотрите логи в консоли разработчика
3. Проверьте серверные логи: `npm run dev` показывает детальную информацию
4. Создайте issue в репозитории проекта
