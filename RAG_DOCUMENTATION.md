# 🤖 RAG (Retrieval-Augmented Generation) - Полная документация

## 📖 Оглавление

1. [Что такое RAG](#что-такое-rag)
2. [Архитектура системы](#архитектура-системы)
3. [Установка и настройка](#установка-и-настройка)
4. [Использование](#использование)
5. [API Reference](#api-reference)
6. [Режимы работы](#режимы-работы)
7. [Сравнение с/без RAG](#сравнение-с-без-rag)
8. [Примеры использования](#примеры-использования)
9. [Troubleshooting](#troubleshooting)

---

## Что такое RAG?

**RAG** (Retrieval-Augmented Generation) - это техника улучшения ответов AI моделей путём предоставления им релевантного контекста из ваших документов.

### Как это работает:

```
Без RAG:
Вопрос → LLM → Ответ (из общих знаний)

С RAG:
Вопрос → Поиск в документах → Релевантные чанки → 
→ LLM (вопрос + контекст) → Точный ответ на основе ваших данных
```

### Преимущества RAG:

✅ **Точность**: Ответы основаны на ваших документах  
✅ **Актуальность**: Работает с вашими данными  
✅ **Проверяемость**: Можно увидеть источники  
✅ **Специфичность**: Знает про ваш проект/компанию  
✅ **Контроль**: Вы контролируете базу знаний  

### Когда использовать RAG:

- ✅ Вопросы про документацию
- ✅ Поиск специфичной информации
- ✅ Внутренняя база знаний компании
- ✅ Технические руководства
- ✅ FAQ системы

### Когда НЕ нужен RAG:

- ❌ Общие вопросы (что такое Python?)
- ❌ Креативные задачи (напиши стих)
- ❌ Математика и логика
- ❌ Программирование с нуля

---

## Архитектура системы

### Компоненты:

```
┌─────────────────────────────────────────────┐
│           Пользователь (UI)                 │
└────────────────┬────────────────────────────┘
                 │ Вопрос
                 ↓
┌─────────────────────────────────────────────┐
│         API Endpoint (/api/rag)             │
│  - Валидация запроса                        │
│  - Маршрутизация по режимам                 │
└────────────────┬────────────────────────────┘
                 │
                 ↓
┌─────────────────────────────────────────────┐
│      Semantic Search (lib/similarity.ts)    │
│  - Генерация embedding для вопроса          │
│  - Поиск похожих чанков                     │
│  - Ранжирование по релевантности            │
└────────────────┬────────────────────────────┘
                 │ Найденные чанки
                 ↓
┌─────────────────────────────────────────────┐
│         RAG Module (lib/rag.ts)             │
│  - Форматирование контекста                 │
│  - Оценка качества                          │
│  - Подготовка для LLM                       │
└────────────────┬────────────────────────────┘
                 │ Отформатированный контекст
                 ↓
┌─────────────────────────────────────────────┐
│         LLM Module (lib/llm.ts)             │
│  - Отправка в Claude API                    │
│  - Обработка ответа                         │
│  - Подсчёт токенов                          │
└────────────────┬────────────────────────────┘
                 │ Ответ от Claude
                 ↓
┌─────────────────────────────────────────────┐
│              Результат                       │
│  - Ответ + Источники + Метаданные          │
└─────────────────────────────────────────────┘
```

### Файловая структура:

```
app/
├── api/
│   └── rag/
│       ├── route.ts           # Основной RAG endpoint
│       └── compare/
│           └── route.ts       # Сравнение режимов
└── page.tsx                   # UI с 3 режимами

lib/
├── llm.ts                     # Claude API интеграция
├── rag.ts                     # RAG логика
├── embeddings.ts              # Генерация embeddings
├── similarity.ts              # Косинусное сходство
└── chunking.ts                # Разбивка документов
```

---

## Установка и настройка

### 1. Получение API ключа

1. Зайдите на https://console.anthropic.com/
2. Создайте аккаунт (есть бесплатные $5)
3. Settings → API Keys → Create Key
4. Скопируйте ключ (начинается с `sk-ant-`)

### 2. Настройка проекта

```bash
# Создайте .env.local в корне проекта
echo "ANTHROPIC_API_KEY=sk-ant-ваш-ключ" > .env.local

# Перезапустите сервер
npm run dev
```

### 3. Индексация документов

Перед использованием RAG нужно создать индекс:

```bash
# Через UI: Нажмите "Переиндексировать"

# Или через API:
curl -X POST http://localhost:3000/api/index
```

### 4. Проверка работоспособности

```bash
# Тестовый запрос
curl -X POST http://localhost:3000/api/rag \
  -H "Content-Type: application/json" \
  -d '{"query": "Привет", "useRAG": false}'
```

Если видите ответ - всё работает! ✅

---

## Использование

### Web UI

#### Режим 1: Поиск 🔍
Классический семантический поиск по документам.

**Когда использовать:**
- Нужно найти конкретные фрагменты
- Хотите увидеть все релевантные места
- Не нужен связный ответ

#### Режим 2: RAG 🤖
Вопрос-ответ с использованием AI.

**Переключатель RAG:**
- ☑️ **Вкл**: Ответ на основе ваших документов
- ☐ **Выкл**: Ответ из общих знаний модели

**Когда использовать:**
- Нужен связный ответ
- Вопрос требует объяснения
- Хотите получить сводку информации

#### Режим 3: Сравнение ⚖️
Запускает оба режима (с RAG и без) параллельно.

**Когда использовать:**
- Хотите увидеть разницу
- Оцениваете полезность RAG
- Тестируете систему

### Через API

См. раздел [API Reference](#api-reference)

---

## API Reference

### POST /api/rag

Основной RAG endpoint для генерации ответов.

**Request:**
```json
{
  "query": "Что такое машинное обучение?",
  "useRAG": true,
  "top_k": 5,
  "min_score": 0.3
}
```

**Parameters:**

| Параметр | Тип | Обязательный | По умолчанию | Описание |
|----------|-----|--------------|--------------|----------|
| `query` | string | Да | - | Вопрос пользователя |
| `useRAG` | boolean | Да | - | Использовать RAG или нет |
| `top_k` | number | Нет | 5 | Количество чанков для поиска |
| `min_score` | number | Нет | 0.3 | Минимальная релевантность |

**Response (с RAG):**
```json
{
  "success": true,
  "query": "Что такое машинное обучение?",
  "answer": "Согласно вашим документам, машинное обучение...",
  "mode": "with_rag",
  "rag_info": {
    "sources": {
      "total_sources": 2,
      "sources": [
        {
          "filename": "ml-guide.md",
          "chunks_used": 3,
          "avg_relevance": 0.85,
          "max_relevance": 0.92
        }
      ]
    },
    "context_quality": {
      "quality": "high",
      "confidence": 0.9,
      "recommendation": "Найдена высокорелевантная информация"
    }
  },
  "metadata": {
    "duration_seconds": 1.234,
    "llm_usage": {
      "input_tokens": 1500,
      "output_tokens": 300
    }
  }
}
```

**Response (без RAG):**
```json
{
  "success": true,
  "query": "Что такое машинное обучение?",
  "answer": "Машинное обучение - это...",
  "mode": "without_rag",
  "metadata": {
    "duration_seconds": 0.8,
    "llm_usage": {
      "input_tokens": 20,
      "output_tokens": 150
    }
  }
}
```

---

### POST /api/rag/compare

Сравнение режимов: запускает оба параллельно.

**Request:**
```json
{
  "query": "Что такое машинное обучение?",
  "top_k": 5,
  "min_score": 0.3
}
```

**Response:**
```json
{
  "success": true,
  "query": "Что такое машинное обучение?",
  "results": {
    "with_rag": {
      "success": true,
      "answer": "...",
      "sources": { ... }
    },
    "without_rag": {
      "success": true,
      "answer": "..."
    }
  },
  "comparison": {
    "both_successful": true,
    "answers_differ": true,
    "analysis": {
      "rag_answer_length": 450,
      "no_rag_answer_length": 280,
      "rag_used_sources": 2,
      "rag_context_quality": "high",
      "recommendation": "RAG значительно улучшил ответ..."
    }
  },
  "metadata": {
    "duration_seconds": 2.1,
    "timestamp": "2024-12-22T..."
  }
}
```

---

## Режимы работы

### Режим 1: С RAG (useRAG: true)

**Процесс:**
1. Вопрос пользователя
2. Генерация embedding
3. Поиск похожих чанков
4. Оценка качества найденного
5. Форматирование контекста
6. Отправка в Claude (вопрос + контекст)
7. Получение ответа

**Преимущества:**
- ✅ Точные ответы из ваших документов
- ✅ Можно проверить источники
- ✅ Работает с специфичной информацией

**Недостатки:**
- ⚠️ Медленнее (~1-3 секунды)
- ⚠️ Больше токенов (дороже)
- ⚠️ Зависит от качества индекса

**Когда использовать:**
- Вопросы про ваши документы
- Нужна точная информация
- Специфичные технические вопросы

---

### Режим 2: Без RAG (useRAG: false)

**Процесс:**
1. Вопрос пользователя
2. Прямая отправка в Claude
3. Получение ответа

**Преимущества:**
- ✅ Быстрее (~0.5-1 секунда)
- ✅ Меньше токенов (дешевле)
- ✅ Хорошо для общих вопросов

**Недостатки:**
- ⚠️ Может не знать про ваши данные
- ⚠️ Ответы из общих знаний модели
- ⚠️ Нет проверки источников

**Когда использовать:**
- Общие вопросы
- Креативные задачи
- Программирование
- Объяснение концепций

---

## Сравнение с/без RAG

### Пример 1: Вопрос про документацию

**Вопрос:** "Как настроить Koin для Android?"

**БЕЗ RAG:**
```
Koin настраивается следующим образом:
1. Добавьте зависимость в build.gradle
2. Создайте модули
3. Инициализируйте в Application классе
[общая информация]
```

**С RAG:**
```
Согласно вашей документации:
1. Добавьте зависимость: implementation "io.insert-koin:koin-android:3.5.0"
2. В вашем проекте модули находятся в di/ папке
3. Инициализация происходит в MyApplication.kt
[точная информация из ваших файлов]

Источники: android-setup.md, koin-config.md
```

**Вывод:** ✅ RAG помог - дал специфичную информацию

---

### Пример 2: Общий вопрос

**Вопрос:** "Что такое циклы в программировании?"

**БЕЗ RAG:**
```
Циклы - это конструкции для повторения кода.
Основные типы: for, while, do-while.
[подробное объяснение]
```

**С RAG:**
```
В ваших документах циклы упоминаются в контексте...
[пытается найти связь с документами]
```

**Вывод:** ❌ RAG не помог - вопрос слишком общий

---

### Пример 3: Специфичный вопрос про проект

**Вопрос:** "Какая архитектура используется в нашем Android приложении?"

**БЕЗ RAG:**
```
Обычно используется MVC, MVP или MVVM.
Современные приложения часто используют Clean Architecture.
[общая информация]
```

**С RAG:**
```
В вашем проекте используется Clean Architecture + MVI pattern.
Структура:
- data/ - репозитории и API
- domain/ - use cases и модели
- presentation/ - UI с MVI

Источник: project-architecture.md
```

**Вывод:** ✅ RAG критически важен - дал точную информацию про проект

---

## Примеры использования

### cURL примеры

```bash
# Простой вопрос с RAG
curl -X POST http://localhost:3000/api/rag \
  -H "Content-Type: application/json" \
  -d '{
    "query": "Что такое машинное обучение?",
    "useRAG": true
  }'

# Вопрос без RAG
curl -X POST http://localhost:3000/api/rag \
  -H "Content-Type: application/json" \
  -d '{
    "query": "Объясни рекурсию простыми словами",
    "useRAG": false
  }'

# Сравнение режимов
curl -X POST http://localhost:3000/api/rag/compare \
  -H "Content-Type: application/json" \
  -d '{
    "query": "Как работает семантический поиск?",
    "top_k": 5
  }'
```

### JavaScript/TypeScript

```typescript
async function askWithRAG(question: string) {
  const response = await fetch('/api/rag', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      query: question,
      useRAG: true,
      top_k: 5,
    }),
  });

  const data = await response.json();
  console.log('Ответ:', data.answer);
  console.log('Источники:', data.rag_info?.sources);
  
  return data;
}

// Использование
const result = await askWithRAG('Что такое Next.js?');
```

### Python

```python
import requests

def ask_with_rag(question: str, use_rag: bool = True):
    response = requests.post(
        'http://localhost:3000/api/rag',
        json={
            'query': question,
            'useRAG': use_rag,
            'top_k': 5
        }
    )
    
    data = response.json()
    print(f"Ответ: {data['answer']}")
    
    if use_rag and data.get('rag_info'):
        print(f"Источников: {data['rag_info']['sources']['total_sources']}")
    
    return data

# Использование
result = ask_with_rag("Что такое машинное обучение?")
```

---

## Troubleshooting

### Проблема: "ANTHROPIC_API_KEY не установлен"

**Решение:**
```bash
# Проверьте файл
cat .env.local

# Добавьте ключ
echo "ANTHROPIC_API_KEY=sk-ant-ваш-ключ" > .env.local

# Перезапустите
npm run dev
```

### Проблема: "Индекс не найден"

**Решение:**
```bash
# Создайте индекс через API
curl -X POST http://localhost:3000/api/index

# Или через UI: нажмите "Переиндексировать"
```

### Проблема: "Не найдено релевантных документов"

**Причины:**
1. Вопрос не связан с документами
2. Слишком высокий `min_score`
3. Мало документов в индексе

**Решение:**
```json
{
  "query": "ваш вопрос",
  "useRAG": true,
  "min_score": 0.1  // Понизьте порог
}
```

### Проблема: Медленные ответы

**Причины:**
- Слишком много чанков (`top_k` большой)
- Медленный интернет
- Большой контекст

**Решение:**
```json
{
  "query": "ваш вопрос",
  "top_k": 3,  // Уменьшите количество чанков
  "useRAG": true
}
```

### Проблема: Claude API error 401

**Причина:** Неверный API ключ

**Решение:**
1. Проверьте ключ на console.anthropic.com
2. Убедитесь что ключ активен
3. Проверьте нет ли лишних пробелов

### Проблема: Rate limit exceeded

**Причина:** Слишком много запросов

**Решение:**
- Подождите 1 минуту
- Добавьте Rate Limiting в код
- Проверьте лимиты на console.anthropic.com

---

## Стоимость использования

### Claude 3.5 Sonnet (текущая модель)

- **Входные токены**: $3 за 1M токенов
- **Выходные токены**: $15 за 1M токенов

### Примерные расчёты:

**Без RAG:**
- Вопрос: ~20 токенов
- Ответ: ~300 токенов
- **Стоимость**: ~$0.005 за запрос

**С RAG:**
- Вопрос + Контекст: ~2000 токенов
- Ответ: ~500 токенов
- **Стоимость**: ~$0.01 за запрос

**Итого:** RAG режим ~в 2 раза дороже, но всё равно очень дёшево!

**100 запросов с RAG ≈ $1** 🎉

---

## Best Practices

### 1. Выбор режима

```typescript
// Используйте функцию для автоопределения
function shouldUseRAG(query: string): boolean {
  const needsRAG = [
    'как настроить',
    'в нашем проекте',
    'согласно документации',
    'по инструкции',
  ];
  
  return needsRAG.some(phrase => 
    query.toLowerCase().includes(phrase)
  );
}
```

### 2. Кэширование результатов

```typescript
const cache = new Map();

async function askWithCache(query: string) {
  const key = `${query}-${useRAG}`;
  
  if (cache.has(key)) {
    return cache.get(key);
  }
  
  const result = await askRAG(query);
  cache.set(key, result);
  
  return result;
}
```

### 3. Оптимизация токенов

- Используйте `top_k: 3-5` вместо 10+
- Повышайте `min_score` для уменьшения контекста
- Для простых вопросов отключайте RAG

---

## Заключение

RAG - мощная техника для работы с вашими документами. Используйте:

- ✅ **С RAG** для специфичных вопросов про ваши данные
- ✅ **Без RAG** для общих вопросов и объяснений
- ✅ **Сравнение** для оценки полезности RAG

**Экспериментируйте и находите оптимальный баланс!** 🚀
