# 🔍 Semantic Search - AI-powered Document Search

Приложение для семантического поиска по markdown документам с использованием локальных эмбеддингов.

## 🚀 Технологии

- **Next.js 14** (App Router)
- **TypeScript**
- **@xenova/transformers** - локальная генерация эмбеддингов (модель Xenova/all-MiniLM-L6-v2)
- **Tailwind CSS** - стилизация UI

## 📋 Возможности

✅ Индексация markdown документов с разбивкой на чанки  
✅ Генерация эмбеддингов локально (без внешних API)  
✅ Семантический поиск с ранжированием по релевантности  
✅ Web UI для удобного использования  
✅ REST API для интеграции  

## 🛠 Установка

### 1. Клонирование и установка зависимостей

```bash
# Перейдите в директорию проекта
cd semantic-search-app

# Установите зависимости
npm install
```

### 2. Добавьте документы

Создайте папку для документов и добавьте туда .md файлы:

```bash
mkdir -p data/documents
```

Добавьте ваши markdown файлы в `data/documents/`. Пример:

```bash
echo "# Тестовый документ

Это пример текста для тестирования семантического поиска.
Система разобьёт этот текст на чанки и создаст эмбеддинги." > data/documents/test.md
```

### 3. Запуск

```bash
# Режим разработки
npm run dev

# Продакшн сборка
npm run build
npm start
```

Приложение будет доступно по адресу: http://localhost:3000

## 📖 Использование

### Web UI

1. Откройте http://localhost:3000
2. Нажмите кнопку **"Переиндексировать"** для создания индекса
3. Введите поисковый запрос и нажмите **"Найти"**

### API

#### Индексация документов

```bash
# POST /api/index - запустить индексацию
curl -X POST http://localhost:3000/api/index

# GET /api/index - получить информацию об индексе
curl http://localhost:3000/api/index
```

Ответ при успешной индексации:
```json
{
  "success": true,
  "message": "Индексация завершена успешно",
  "stats": {
    "total_chunks": 45,
    "total_documents": 3,
    "documents": ["doc1.md", "doc2.md", "doc3.md"],
    "model": "Xenova/all-MiniLM-L6-v2",
    "duration_seconds": 12.5,
    "indexed_at": "2024-12-22T10:30:00.000Z"
  }
}
```

#### Поиск

```bash
# POST /api/search - выполнить поиск
curl -X POST http://localhost:3000/api/search \
  -H "Content-Type: application/json" \
  -d '{
    "query": "семантический поиск",
    "top_k": 5,
    "min_score": 0.3
  }'

# GET /api/search - получить справку по API
curl http://localhost:3000/api/search
```

Пример ответа:
```json
{
  "success": true,
  "query": "семантический поиск",
  "results": [
    {
      "id": "abc-123",
      "text": "Семантический поиск использует эмбеддинги...",
      "source": "doc1.md",
      "score": 0.8523,
      "metadata": {
        "position": 1,
        "totalChunks": 10
      }
    }
  ],
  "stats": {
    "total_results": 5,
    "avg_score": 0.6234,
    "max_score": 0.8523,
    "min_score": 0.3145,
    "duration_seconds": 0.234
  }
}
```

#### Параметры поиска

| Параметр | Тип | Обязательный | По умолчанию | Описание |
|----------|-----|--------------|--------------|----------|
| `query` | string | Да | - | Поисковый запрос |
| `top_k` | number | Нет | 5 | Количество результатов (1-100) |
| `min_score` | number | Нет | 0.3 | Минимальный порог релевантности (0-1) |

## 📁 Структура проекта

```
semantic-search-app/
├── app/
│   ├── api/
│   │   ├── index/
│   │   │   └── route.ts       # API индексации
│   │   └── search/
│   │       └── route.ts       # API поиска
│   ├── globals.css            # Глобальные стили
│   ├── layout.tsx             # Корневой layout
│   └── page.tsx               # Главная страница (UI)
├── lib/
│   ├── embeddings.ts          # Генерация эмбеддингов
│   ├── chunking.ts            # Разбивка текста на чанки
│   ├── similarity.ts          # Вычисление косинусного сходства
│   └── types.ts               # TypeScript типы
├── data/
│   ├── documents/             # MD файлы (добавьте свои)
│   └── index.json             # Сгенерированный индекс
├── package.json
├── tsconfig.json
├── tailwind.config.js
└── README.md
```

## 🔧 Как это работает

### 1. Индексация

1. **Чтение файлов**: Система читает все .md файлы из `data/documents/`
2. **Разбивка на чанки**: Текст разбивается на фрагменты ~500 символов с перекрытием 50 символов
3. **Генерация эмбеддингов**: Для каждого чанка генерируется векторное представление (384 измерения)
4. **Сохранение**: Индекс сохраняется в `data/index.json`

### 2. Поиск

1. **Генерация эмбеддинга запроса**: Создаётся векторное представление поискового запроса
2. **Вычисление сходства**: Рассчитывается косинусное сходство с каждым чанком
3. **Ранжирование**: Результаты сортируются по релевантности
4. **Фильтрация**: Возвращаются топ-K результатов выше порога

## 🎯 Настройка

### Изменение параметров разбивки

В `lib/chunking.ts`:

```typescript
const DEFAULT_CONFIG: ChunkingConfig = {
  chunkSize: 500,      // Размер чанка
  overlap: 50,         // Перекрытие
  minChunkSize: 100,   // Минимальный размер
};
```

### Использование другой модели

В `lib/embeddings.ts`:

```typescript
const MODEL_NAME = 'Xenova/all-MiniLM-L6-v2';  // Замените на другую модель
```

Доступные модели: https://huggingface.co/models?library=transformers.js

## ⚡ Производительность

- **Индексация**: ~2-5 секунд на документ (зависит от размера)
- **Поиск**: ~200-500 мс (включая генерацию эмбеддинга запроса)
- **Размер модели**: ~23 МБ (кэшируется локально)

## 🐛 Решение проблем

### Ошибка: "Директория не найдена"
```bash
mkdir -p data/documents
```

### Ошибка: "Cannot find module @xenova/transformers"
```bash
npm install
```

### Медленная индексация
- Используйте более мощное железо
- Уменьшите размер чанков
- Рассмотрите переход на GPU для больших объёмов

### Плохие результаты поиска
- Убедитесь, что документы содержат релевантную информацию
- Попробуйте другие формулировки запроса
- Уменьшите `min_score` для большего охвата
- Увеличьте `top_k` для большего количества результатов

## 📝 Примеры запросов

**Поиск по теме:**
```json
{
  "query": "машинное обучение и нейронные сети",
  "top_k": 10
}
```

**Поиск с высокой точностью:**
```json
{
  "query": "установка зависимостей npm",
  "top_k": 3,
  "min_score": 0.6
}
```

## 🔒 Безопасность

- Модель работает локально, данные не покидают ваш сервер
- Нет внешних API вызовов
- Индекс хранится в файловой системе

## 📄 Лицензия

MIT

## 🤝 Вклад в проект

Приветствуются pull requests и issue!

## 📞 Поддержка

При возникновении проблем создайте issue в репозитории.
