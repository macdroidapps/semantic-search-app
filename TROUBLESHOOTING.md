# 🐛 Troubleshooting - Решение проблем

## ❌ Ошибка 500 (Internal Server Error)

### Симптомы:
- В консоли браузера: "Failed to load resource: 500"
- RAG режим не работает
- Ошибка при попытке спросить у AI

### Причина №1: API ключ не настроен (90% случаев)

**Проверка:**
```bash
# Проверьте файл .env.local
cat .env.local

# Должна быть строка:
# ANTHROPIC_API_KEY=sk-ant-xxxxx...
```

**Решение:**

1. Получите API ключ:
   - Зайдите на https://console.anthropic.com/
   - Создайте аккаунт (бесплатные $5!)
   - Settings → API Keys → Create Key
   - Скопируйте ключ (начинается с `sk-ant-`)

2. Создайте файл `.env.local` в корне проекта:
   ```bash
   cd semantic-search-app
   echo "ANTHROPIC_API_KEY=sk-ant-ваш-ключ-здесь" > .env.local
   ```

3. **ОБЯЗАТЕЛЬНО** перезапустите сервер:
   ```bash
   # Остановите сервер (Ctrl+C)
   # Затем запустите снова
   npm run dev
   ```

4. Проверьте что ключ загружен:
   ```bash
   # Откройте отдельный терминал
   curl http://localhost:3000/api/rag/status
   
   # Должно быть: "anthropic_api": { "configured": true }
   ```

---

### Причина №2: Индекс не создан

**Симптомы:**
- Ошибка: "Индекс не найден"
- Ошибка: "Не найдено релевантных документов"

**Решение:**

1. Откройте http://localhost:3000
2. Нажмите кнопку **"🔄 Переиндексировать"**
3. Подождите завершения (5-10 секунд)
4. Попробуйте снова

Или через API:
```bash
curl -X POST http://localhost:3000/api/index
```

---

### Причина №3: Нет документов

**Симптомы:**
- Индекс создаётся, но пустой
- "Total documents: 0"

**Решение:**

1. Проверьте папку с документами:
   ```bash
   ls data/documents/
   ```

2. Должны быть .md файлы. Если нет - добавьте:
   ```bash
   # Примеры документов уже должны быть
   # Если их нет, скачайте проект заново
   ```

3. Переиндексируйте после добавления файлов

---

## 🔍 Диагностика проблем

### Проверка статуса системы:

```bash
curl http://localhost:3000/api/rag/status
```

Ответ покажет:
- ✅ Что настроено правильно
- ❌ Что нужно исправить
- 💡 Пошаговые инструкции

**Пример ответа:**

```json
{
  "ready": false,
  "components": {
    "anthropic_api": {
      "configured": false,
      "status": "not_configured"
    },
    "search_index": {
      "exists": true,
      "status": "ready"
    }
  },
  "errors": [
    {
      "component": "anthropic_api",
      "message": "ANTHROPIC_API_KEY не установлен",
      "solution": "Создайте .env.local и добавьте ключ"
    }
  ]
}
```

---

## 🎯 Быстрая проверка (чек-лист)

Выполните по порядку:

### 1. Проверьте API ключ:
```bash
cat .env.local | grep ANTHROPIC_API_KEY
```
✅ Есть → Идите к шагу 2  
❌ Нет → Создайте `.env.local` с ключом, перезапустите сервер

### 2. Проверьте индекс:
```bash
ls data/index.json
```
✅ Есть → Идите к шагу 3  
❌ Нет → Нажмите "Переиндексировать" в UI

### 3. Проверьте документы:
```bash
ls data/documents/*.md
```
✅ Есть → Идите к шагу 4  
❌ Нет → Добавьте .md файлы

### 4. Проверьте работу:
```bash
curl -X POST http://localhost:3000/api/rag \
  -H "Content-Type: application/json" \
  -d '{"query": "Привет", "useRAG": false}'
```
✅ Работает → Готово!  
❌ Ошибка → Смотрите ниже

---

## 📝 Часто задаваемые вопросы

### Q: Сервер перезапустился, но ошибка осталась

**A:** Убедитесь что файл называется `.env.local` (с точкой вначале), а не `env.local`

```bash
# Правильно:
ls -la | grep .env.local

# Должно показать:
# .env.local
```

---

### Q: API ключ точно правильный, но не работает

**A:** Проверьте:

1. Нет лишних пробелов:
   ```bash
   # НЕПРАВИЛЬНО (пробел после ключа):
   ANTHROPIC_API_KEY=sk-ant-xxx 
   
   # ПРАВИЛЬНО:
   ANTHROPIC_API_KEY=sk-ant-xxx
   ```

2. Ключ активен на https://console.anthropic.com/

3. Нет кавычек:
   ```bash
   # НЕПРАВИЛЬНО:
   ANTHROPIC_API_KEY="sk-ant-xxx"
   
   # ПРАВИЛЬНО:
   ANTHROPIC_API_KEY=sk-ant-xxx
   ```

---

### Q: Режим "Поиск" работает, а "RAG" нет

**A:** Это нормально! Режим "Поиск" не требует API ключа.  
Для RAG нужен ANTHROPIC_API_KEY.

---

### Q: Получаю "Rate limit exceeded"

**A:** Слишком много запросов.  
Решение: подождите 1 минуту и попробуйте снова.

---

### Q: "Claude API error: 401"

**A:** Неверный API ключ.

Решение:
1. Зайдите на https://console.anthropic.com/
2. Settings → API Keys
3. Проверьте что ключ активен
4. Создайте новый ключ если нужно
5. Обновите .env.local
6. Перезапустите сервер

---

## 🔧 Дополнительная диагностика

### Проверка логов сервера:

Смотрите в терминале где запущен `npm run dev`:

**Хорошие логи:**
```
[API RAG] Запрос: "Что такое ML..."
[API RAG] Загрузка индекса...
[API RAG] Поиск релевантных чанков...
[API RAG] Найдено 5 релевантных чанков
[LLM] Отправка запроса к Claude API...
[LLM] Ответ получен
```

**Плохие логи (ошибки):**
```
[API RAG] Ошибка: ANTHROPIC_API_KEY не установлен
→ Решение: настройте API ключ

[API RAG] Ошибка: Индекс не найден
→ Решение: создайте индекс

[LLM] Ошибка: Claude API error: 401
→ Решение: проверьте API ключ
```

---

## 🚑 Экстренное решение

Если ничего не помогает:

### 1. Полная переустановка:

```bash
# Остановите сервер (Ctrl+C)

# Удалите node_modules и package-lock.json
rm -rf node_modules package-lock.json

# Установите заново
npm install

# Проверьте .env.local
cat .env.local

# Если файла нет - создайте:
echo "ANTHROPIC_API_KEY=sk-ant-ваш-ключ" > .env.local

# Запустите
npm run dev
```

### 2. Используйте только режим "Поиск":

Если RAG не критичен:
1. Переключитесь на вкладку **"🔍 Поиск"**
2. Этот режим работает без API ключа
3. Вы можете искать по документам

### 3. Тестовый запрос:

```bash
# Проверьте что сервер вообще работает
curl http://localhost:3000/api/index

# Должен вернуть JSON с информацией об индексе
```

---

## 📞 Всё ещё не работает?

1. Проверьте версию Node.js:
   ```bash
   node -v
   # Должна быть 18+ или выше
   ```

2. Проверьте что порт 3000 не занят:
   ```bash
   lsof -i :3000
   # Если порт занят - остановите процесс или используйте другой порт:
   PORT=3001 npm run dev
   ```

3. Посмотрите полную документацию:
   - `RAG_QUICKSTART.md` - быстрый старт
   - `RAG_DOCUMENTATION.md` - полная документация

---

## ✅ После исправления

1. Перезапустите сервер (Ctrl+C → npm run dev)
2. Откройте http://localhost:3000
3. Проверьте статус в панели "📊 Статус системы"
4. Попробуйте режим "🤖 RAG"
5. Если работает - готово! 🎉

---

## 💡 Профилактика

Чтобы избежать проблем:

1. **Всегда перезапускайте сервер** после изменения .env.local
2. **Храните резервную копию** .env.local
3. **Проверяйте лимиты** API на console.anthropic.com
4. **Переиндексируйте** после добавления новых документов
5. **Читайте логи** в терминале для диагностики

---

**Нужна помощь?** Читайте полную документацию в `RAG_DOCUMENTATION.md`
