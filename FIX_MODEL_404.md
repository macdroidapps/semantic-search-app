# 🔧 Исправление: Error 404 - Model Not Found

## ❌ Ошибка:

```
Claude API error: 404 - model: claude-3-5-sonnet-20241022
type: 'not_found_error'
```

## ✅ Решение:

Модель `claude-3-5-sonnet-20241022` не найдена. Нужно использовать другую модель.

---

## 🚀 Быстрое исправление (2 минуты):

### Вариант 1: Через .env.local (рекомендуется)

```bash
# Откройте файл .env.local
nano .env.local

# Добавьте строку с моделью:
CLAUDE_MODEL=claude-3-5-sonnet-20240620

# Сохраните (Ctrl+O, Enter, Ctrl+X)

# Перезапустите сервер
npm run dev
```

### Вариант 2: Редактировать напрямую

Откройте файл `lib/config.ts` и измените строку:

```typescript
// Было:
export const DEFAULT_MODEL = 'claude-3-5-sonnet-20241022';

// Стало:
export const DEFAULT_MODEL = 'claude-3-5-sonnet-20240620';
```

Перезапустите сервер: `npm run dev`

---

## 📋 Доступные модели:

| Модель | Описание | Скорость | Цена |
|--------|----------|----------|------|
| `claude-3-5-sonnet-20240620` | ✅ Стабильная, рекомендуется | ⚡⚡⚡ | $$ |
| `claude-3-sonnet-20240229` | Баланс качества и скорости | ⚡⚡ | $$ |
| `claude-3-haiku-20240307` | Самая быстрая и дешёвая | ⚡⚡⚡⚡ | $ |
| `claude-3-opus-20240229` | Самая умная, но медленная | ⚡ | $$$$ |

---

## 💡 Как выбрать модель?

### Для продакшена (рекомендуется):
```bash
CLAUDE_MODEL=claude-3-5-sonnet-20240620
```
- Стабильная версия
- Хорошее качество ответов
- Нормальная скорость

### Для экспериментов (самая быстрая):
```bash
CLAUDE_MODEL=claude-3-haiku-20240307
```
- В 4 раза быстрее
- В 6 раз дешевле
- Подходит для простых задач

### Для сложных задач:
```bash
CLAUDE_MODEL=claude-3-opus-20240229
```
- Самое высокое качество
- Медленнее
- Дороже

---

## 🔍 Проверка:

После изменения модели проверьте:

```bash
# Перезапустите сервер
npm run dev

# В логах должно быть:
[Config] Используется модель из .env: claude-3-5-sonnet-20240620
# или
[LLM] Модель: claude-3-5-sonnet-20240620

# Попробуйте запрос
curl -X POST http://localhost:3000/api/rag \
  -H "Content-Type: application/json" \
  -d '{"query": "Привет", "useRAG": false}'
```

Должно вернуть ответ без ошибок! ✅

---

## 📝 Полный .env.local пример:

```bash
# API ключ (обязательно)
ANTHROPIC_API_KEY=sk-ant-ваш-ключ-здесь

# Модель (опционально, по умолчанию claude-3-5-sonnet-20240620)
CLAUDE_MODEL=claude-3-5-sonnet-20240620
```

---

## ⚠️ Если всё ещё не работает:

1. **Проверьте API ключ:**
   ```bash
   # Зайдите на console.anthropic.com
   # Settings → API Keys
   # Убедитесь что ключ активен
   ```

2. **Проверьте баланс:**
   - Зайдите на https://console.anthropic.com/
   - Settings → Billing
   - Убедитесь что есть кредиты ($5 бесплатно при регистрации)

3. **Попробуйте другую модель:**
   ```bash
   # Haiku - самая стабильная
   CLAUDE_MODEL=claude-3-haiku-20240307
   ```

4. **Проверьте логи:**
   - Смотрите терминал где запущен `npm run dev`
   - Должно быть: `[LLM] Модель: claude-3-...`
   - Не должно быть: `404` или `not_found_error`

---

## 🎉 Готово!

После исправления:
1. Перезапустите сервер
2. Попробуйте RAG режим
3. Должно работать!

**Рекомендуемая модель:** `claude-3-5-sonnet-20240620` ✅

---

## 📚 Дополнительно:

Все доступные модели и их параметры смотрите в:
- `lib/config.ts` - конфигурация моделей
- https://docs.anthropic.com/en/docs/models-overview - официальная документация
