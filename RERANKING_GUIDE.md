# 🎯 Реранкинг - Полный гайд

## 📖 Что такое реранкинг?

**Реранкинг** - это второй этап обработки результатов поиска, который улучшает их качество путём более точной оценки релевантности.

### Как работает RAG без реранкинга:

```
Запрос → Embeddings → Поиск top-5 → LLM → Ответ
         (быстро)     (косинусное сходство)
```

**Проблема:** Косинусное сходство не всегда точно оценивает релевантность.

### Как работает RAG с реранкингом:

```
Запрос → Embeddings → Поиск top-20 → Реранкинг → Топ-5 лучших → LLM → Ответ
         (быстро)     (первичный)    (умный!)     (точные!)
```

**Преимущество:** Более точный отбор релевантных чанков!

---

## 🎨 Методы реранкинга

### 1. None (Без реранкинга) - Baseline

**Как работает:**
- Используется только косинусное сходство
- Самый быстрый метод
- Базовая точность

**Когда использовать:**
- Когда скорость критична
- Для простых запросов
- Когда качество уже достаточное

---

### 2. Keyword Boost (Ключевые слова)

**Как работает:**
1. Извлекает ключевые слова из запроса
2. Проверяет их наличие в документах
3. Повышает score при совпадениях
4. Формула: `rerank_score = cosine_score * 0.7 + keyword_match * 0.3`

**Пример:**

```
Запрос: "PostgreSQL база данных"
Ключевые слова: [postgresql, база, данных]

Чанк 1: "PostgreSQL 15 используется как основная база данных"
→ Найдено 3/3 ключевых слов → +0.3 к score

Чанк 2: "Система использует реляционную базу"
→ Найдено 1/3 ключевых слов → +0.1 к score
```

**Преимущества:**
- ✅ Быстрый
- ✅ Хорошо для точных запросов
- ✅ Находит прямые совпадения

**Недостатки:**
- ❌ Не понимает синонимы
- ❌ Может пропустить семантически похожее

**Когда использовать:**
- Технические запросы с конкретными терминами
- Поиск имён, названий, версий
- Когда важны точные совпадения

---

### 3. Semantic Deep (Глубокий семантический)

**Как работает:**
1. Анализирует позицию в документе
2. Проверяет длину текста
3. Ищет конкретные данные (цифры, имена)
4. Считает прямые совпадения слов
5. Суммирует все факторы

**Факторы:**
- **Позиция:** Первый чанк в документе +0.1
- **Длина:** Оптимальная длина (200-500 символов) +0.05
- **Данные:** Наличие цифр, дат, имён +0.05
- **Совпадения:** За каждое прямое совпадение слова +0.05

**Пример:**

```
Чанк: "Проект Foodee разработан Vladimir в 2024. 
       Используется PostgreSQL 15 и Redis."

Анализ:
✅ Позиция: первый чанк → +0.1
✅ Длина: 250 символов → +0.05
✅ Данные: есть имена (Vladimir), цифры (2024, 15) → +0.05
✅ Совпадения: 2 слова → +0.1
────────────────────────────────
Итого: +0.30 к базовому score
```

**Преимущества:**
- ✅ Учитывает контекст
- ✅ Находит важную информацию
- ✅ Предпочитает информативные чанки

**Недостатки:**
- ❌ Медленнее keyword
- ❌ Может переоценить первые чанки

**Когда использовать:**
- Вопросы про людей, даты, цифры
- Когда важен контекст
- Для длинных документов

---

### 4. Hybrid (Гибридный) - **РЕКОМЕНДУЕТСЯ!**

**Как работает:**
1. Применяет оба метода: Keyword + Semantic
2. Комбинирует их scores: `hybrid_score = (keyword * 0.5 + semantic * 0.5)`
3. Получает лучшее из обоих миров

**Преимущества:**
- ✅ Лучшая точность
- ✅ Универсальный
- ✅ Работает для любых запросов
- ✅ Компенсирует недостатки каждого метода

**Недостатки:**
- ⚠️ Немного медленнее (но не критично)

**Когда использовать:**
- **По умолчанию для всех задач!**
- Когда не знаете какой метод лучше
- Для продакшена

---

## 💻 Использование через API

### Базовый запрос (без реранкинга):

```bash
curl -X POST http://localhost:3000/api/rag \
  -H "Content-Type: application/json" \
  -d '{
    "query": "Какая база данных в Foodee?",
    "useRAG": true,
    "rerank": false
  }'
```

### С реранкингом (hybrid по умолчанию):

```bash
curl -X POST http://localhost:3000/api/rag \
  -H "Content-Type: application/json" \
  -d '{
    "query": "Какая база данных в Foodee?",
    "useRAG": true,
    "rerank": true
  }'
```

### С настройками реранкинга:

```bash
curl -X POST http://localhost:3000/api/rag \
  -H "Content-Type: application/json" \
  -d '{
    "query": "Какая база данных в Foodee?",
    "useRAG": true,
    "rerank": true,
    "rerank_config": {
      "rerank_method": "keyword-boost",
      "min_rerank_score": 0.6,
      "top_k_for_rerank": 15,
      "final_top_k": 3
    }
  }'
```

---

## 📊 Сравнение методов

### Endpoint для сравнения:

```bash
curl -X POST http://localhost:3000/api/rag/rerank-compare \
  -H "Content-Type: application/json" \
  -d '{
    "query": "Кто разработчик Foodee?",
    "top_k": 20
  }'
```

### Ответ:

```json
{
  "success": true,
  "query": "Кто разработчик Foodee?",
  "best_method": "hybrid",
  "analysis": {
    "none": {
      "avg_score": 0.45,
      "quality": { "high": 1, "medium": 2, "low": 2 }
    },
    "keyword": {
      "avg_score": 0.58,
      "quality": { "high": 2, "medium": 2, "low": 1 }
    },
    "semantic": {
      "avg_score": 0.62,
      "quality": { "high": 3, "medium": 1, "low": 1 }
    },
    "hybrid": {
      "avg_score": 0.67,
      "quality": { "high": 4, "medium": 1, "low": 0 }
    }
  }
}
```

---

## 🎯 Примеры: Когда какой метод помогает

### Пример 1: Технический запрос

**Запрос:** "PostgreSQL версия"

**Без реранкинга (top-3):**
1. "...база данных..." (score: 0.42) ❌ Не то
2. "PostgreSQL 15..." (score: 0.40) ✅ Нужное!
3. "...система..." (score: 0.38) ❌ Не то

**С keyword-boost (top-3):**
1. "PostgreSQL 15..." (score: 0.68) ✅ Нужное!
2. "PostgreSQL features..." (score: 0.55) ✅ Полезно
3. "...база данных..." (score: 0.42) ⚠️ Окей

**Вывод:** ✅ Keyword-boost сильно помог!

---

### Пример 2: Вопрос про человека

**Запрос:** "Кто создал Foodee?"

**Без реранкинга (top-3):**
1. "Foodee - это..." (score: 0.50) ⚠️ Общее
2. "...проект..." (score: 0.45) ❌ Не то
3. "Vladimir разработчик" (score: 0.42) ✅ Нужное!

**С semantic-deep (top-3):**
1. "Vladimir разработчик" (score: 0.72) ✅ Нужное!
2. "Команда: Vladimir..." (score: 0.65) ✅ Полезно
3. "Foodee - это..." (score: 0.50) ⚠️ Окей

**Вывод:** ✅ Semantic нашёл имена и поднял их!

---

### Пример 3: Сложный запрос

**Запрос:** "Где развёрнут проект и кто администратор?"

**Без реранкинга (top-3):**
1. "...сервер..." (score: 0.48) ⚠️
2. "VPS macdroid.ru" (score: 0.45) ✅ Часть ответа
3. "...проект..." (score: 0.42) ❌

**С hybrid (top-3):**
1. "VPS macdroid.ru, admin: Vladimir" (score: 0.75) ✅ Полный ответ!
2. "Deploy: PM2 на VPS" (score: 0.68) ✅ Детали
3. "Команда: Vladimir..." (score: 0.62) ✅ Про админа

**Вывод:** ✅ Hybrid скомбинировал оба метода!

---

## ⚙️ Настройка порогов

### Параметры конфигурации:

```typescript
{
  // Первичный порог (для косинусного сходства)
  min_similarity: 0.3,  // Понизьте для большего recall
  
  // Вторичный порог (после реранкинга)
  min_rerank_score: 0.5,  // Повысьте для большего precision
  
  // Сколько взять для реранкинга
  top_k_for_rerank: 20,  // Больше = точнее, но медленнее
  
  // Сколько оставить после реранкинга
  final_top_k: 5,  // Оптимально 3-7
  
  // Метод
  rerank_method: 'hybrid',  // Рекомендуется
}
```

### Рекомендации по порогам:

| Сценарий | min_similarity | min_rerank_score | top_k_for_rerank |
|----------|----------------|------------------|------------------|
| **Максимальный recall** | 0.2 | 0.4 | 30 |
| **Баланс** (рекомендуется) | 0.3 | 0.5 | 20 |
| **Максимальный precision** | 0.4 | 0.6 | 15 |
| **Быстрый поиск** | 0.3 | 0.5 | 10 |

---

## 📈 Метрики качества

После реранкинга API возвращает метрики:

```json
{
  "rag_info": {
    "reranking": {
      "avg_score_improvement": 0.15,
      "quality_distribution": {
        "high": 4,
        "medium": 1,
        "low": 0
      },
      "quality_analysis": {
        "improvement": 0.15,
        "top_changed": true,
        "avg_position_change": 2.4
      }
    }
  }
}
```

**Как читать:**
- `avg_score_improvement > 0.1`: Реранкинг сильно помог ✅
- `top_changed: true`: Топ-1 результат изменился
- `high: 4`: Из 5 результатов 4 высокого качества

---

## 🚀 Best Practices

### 1. Выбор метода

```
Технические запросы → keyword-boost
Вопросы про людей/даты → semantic-deep
Не уверен → hybrid
```

### 2. Настройка top_k

```
Мало документов (< 100 чанков) → top_k_for_rerank: 10-15
Средне (100-500) → top_k_for_rerank: 15-20
Много (> 500) → top_k_for_rerank: 20-30
```

### 3. Когда НЕ использовать реранкинг

- ❌ Очень простые запросы (1-2 слова)
- ❌ Когда скорость критична (< 1 секунда)
- ❌ Когда базовый поиск уже даёт отличные результаты

---

## 🎓 Выводы

### Когда реранкинг помогает:

✅ **Сложные запросы** с несколькими аспектами  
✅ **Технические термины** требующие точного совпадения  
✅ **Вопросы про людей, даты, цифры**  
✅ **Когда базовый поиск даёт много "почти релевантных" результатов**  

### Когда реранкинг НЕ помогает:

❌ **Очень простые запросы**  
❌ **Когда в документах нет ответа**  
❌ **Общие вопросы** ("что такое программирование?")  

---

## 💡 Рекомендации

1. **Начните с hybrid** - работает для 95% случаев
2. **Используйте сравнение** - endpoint `/api/rag/rerank-compare` для A/B тестов
3. **Настройте пороги** под свои данные
4. **Мониторьте метрики** - `avg_score_improvement` должен быть > 0.1

---

**Реранкинг - это must-have для продакшн RAG систем!** 🚀
